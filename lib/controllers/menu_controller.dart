import 'package:flutter/material.dart';

class MYMenuController {
  void navigateToPage(BuildContext context, int index) {
    // You can add more complex navigation logic here if needed
    Navigator.of(context).pop();
  }
}